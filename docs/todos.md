
# TODO Workflow

ExecPlans may define tasks.

Tasks may still generate entries in `TODO.md`, but that file is now a projection surface rather than the canonical work authority.

Canonical work state lives in the remaining-work graph and related governed graph artifacts.

States:

draft → ready → in‑progress → review → blocked → done
