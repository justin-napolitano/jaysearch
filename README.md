# Platform Template

Self-governing development template using ExecPlans, TODO workflow, ADRs, and local CI validation.

See docs/ for full usage.

Bootstrap a new project:

`bin/bootstrap-project /tmp --name my-codex-project`
